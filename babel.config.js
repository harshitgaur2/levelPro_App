module.exports = {
    presets: [
      "@babel/preset-env", // You can add other presets as needed
      "@babel/preset-react" // for React projects
    ],
    plugins: [
      "@babel/plugin-proposal-private-property-in-object"
      // Add other plugins as needed
    ]
  };
  